{
  "name": "wamifugo",
  "version": "1.0.0",
  "description": "Wa-Mifugo Feeds Management System",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": "20.x"
  }
}
